# ===========================================
# Cozeal Vouchers - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values

# ===========================================
# MongoDB Configuration
# ===========================================
# For docker-compose (production with auth):
MONGO_USERNAME=admin
MONGO_PASSWORD=change-this-secure-password

# For docker-compose.yml (development, no auth):
# DATABASE_URL=mongodb://mongo:27017/cozeal

# For docker-compose.prod.yml (production with auth):
# DATABASE_URL is auto-configured from MONGO_USERNAME and MONGO_PASSWORD

# For external MongoDB (e.g., MongoDB Atlas):
# DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/cozeal?retryWrites=true&w=majority

# ===========================================
# NextAuth.js Authentication
# ===========================================
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-here
NEXTAUTH_URL=https://your-domain.com

# ===========================================
# Tap Payment Gateway
# ===========================================
# Get your keys from https://dashboard.tap.company
TAP_SECRET_KEY=sk_test_your_tap_secret_key
TAP_PUBLIC_KEY=pk_test_your_tap_public_key

# ===========================================
# Application URL
# ===========================================
# Used for payment redirects
NEXT_PUBLIC_APP_URL=https://your-domain.com

# ===========================================
# Admin Account (created by seed script)
# ===========================================
# Default credentials created by `npm run db:seed` or `./docker-deploy.sh seed`:
#   Email: admin@cozeal.ai
#   Password: Cz@Admin#2026!Secure
#
# ⚠️ IMPORTANT: Change the admin password after first login!
